# Cloudflare Workers + Assets configuration
# Build command: cd quartz && npm install && npx quartz build -d ../content
# Node.js version: 22

name = "memex"
compatibility_date = "2026-03-14"

[assets]
directory = "quartz/public"
